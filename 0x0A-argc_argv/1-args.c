#include <stdio.h>

/**
  * main - program entry point
  * argc: argument count
  * argv: argument count
  * Return: number of argument passed
  */
int main(int argc, char *argv[])
{
	int i = 0;

	if (argv[i] = i)
		printf("%d\n", i);
	else
		printf("%d\n", argc);
	return (0);
}
