#include "main.h"
#include <stdlib.h>

/**
  * _atoi - convert a string to an integer
  * @s: pointer to char
  * Return: Integer
  */
int _atoi(char *s)
{
	int result;

	result = atoi(s);

	return (result);
}
