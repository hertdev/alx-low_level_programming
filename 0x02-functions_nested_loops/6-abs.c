#include "main.h"

/**
  * _abs - Prints the absolute value of an integer
  * @n: Integer value to consider
  * Return: absolute value
  */
int _abs(int n)
{
	int x = abs(n);
	_putchar(x);
	return (0);
}
