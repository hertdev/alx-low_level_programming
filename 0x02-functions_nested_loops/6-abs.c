#include "main.h"

/**
  * _abs - Prints the absolute value of an integer
  * @n: Integer value to consider
  * Return: absolute value
  */
int _abs(int n)
{
	if (n < 0)
		n = n * (-);
	_putchar(n);
	return (0);
}
